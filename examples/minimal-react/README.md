# Minimal React example

Run `pnpm install && pnpm --filter streamtag-ui build` at the repository root, then `pnpm --filter @streamtag-ui/minimal-react dev`.

This example consumes only the package public API. Move the slider to replay accumulated text. No model account or API key is required.
